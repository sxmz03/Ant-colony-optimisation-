# Building Evacuation — Ant Colony Optimisation

ACO implementation that finds the fastest and safest escape route through a building modelled as a weighted directed graph. Ants score paths using a fitness function combining distance and risk per edge, with pheromones reinforcing better routes over iterations.

Two building layouts included (`building1.py`, `building2.py`). Graphs visualised with NetworkX and Matplotlib.

